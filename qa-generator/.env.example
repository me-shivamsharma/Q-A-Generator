# Q&A Generator Application Configuration
# Copy this file to .env.local and configure your settings

# ===== GCP Configuration =====
# Google Cloud Project ID where Secret Manager is configured
GCP_PROJECT_ID=your-gcp-project-id

# GCP Service Account Key Path (for local development)
# Download service account JSON key from GCP Console
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account-key.json

# GCP Secret Manager Configuration
# The secret name in GCP Secret Manager containing the OpenAI API key
SECRET_NAME=Q-A-generator

# ===== Database Configuration =====
# PostgreSQL Database URL
DATABASE_URL=postgresql://username:password@localhost:5432/qa_generator

# ===== Authentication Configuration =====
# JWT Secret for token signing (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-here

# Session secret for cookie signing
SESSION_SECRET=your-session-secret-here

# ===== Application Configuration =====
# Port to run the application on
PORT=9000

# Environment (development, production, test)
NODE_ENV=development

# ===== Security Configuration =====
# CORS origins (comma-separated list for production)
CORS_ORIGINS=http://localhost:9000

# Rate limiting (requests per minute per IP)
RATE_LIMIT_REQUESTS=100

# ===== Optional: Fallback API Keys (for development only) =====
# WARNING: These should NOT be used in production
# Use GCP Secret Manager instead
# OPENAI_API_KEY=your_openai_api_key_here
# GOOGLE_AI_API_KEY=your_google_ai_api_key_here
